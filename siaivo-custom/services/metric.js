import Metric from '../../services/metric'

Metric.init = function () {
    // Skip sending metrics.
}

Metric.counter = function () {
    // Skip sending metrics.
}

Metric.histogram = function () {
    // Skip sending metrics.
}